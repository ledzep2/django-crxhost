from setuptools import setup
setup(name='crxhost',
	version='0.9',
	packages=['crxhost'],
	include_package_data=True,
	license='MIT',
	long_description="A reusable Django app that hosts versioned chrome extension packages."
)
